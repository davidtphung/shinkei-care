Play now: **https://sere.davidtphung.com/**

HTTPS is live (Let's Encrypt, cert approved). Enforce HTTPS is not flipped yet (API 403); tick it on the user site Pages settings if you want HTTP redirected.

Fallback: **https://davidtphung.github.io/shinkei-care/**
